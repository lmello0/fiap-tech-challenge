services:
  postgres:
    image: 'postgres:18'
    env_file:
      - .env
    environment:
      - 'TZ=America/Sao_Paulo'
      - postgres:/var/lib/postgresql/18/docker
    ports:
      - '5432:5432'

volumes:
  postgres:
  mailpit:
