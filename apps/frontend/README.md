# Frontend

Reserved for the Angular app. Not scaffolded yet.

The backend's `APP_BASE_URL` already defaults to `http://localhost:4200` (the Angular CLI dev
server default) — see `apps/backend/.env.example`.

Once scaffolded, `docker compose --profile frontend up` runs it via the `frontend` service in the
root `compose.yaml`.
