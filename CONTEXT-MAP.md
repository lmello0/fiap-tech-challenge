# Context Map

- [Auto Repair Shop domain model](apps/backend/CONTEXT.md) — `apps/backend/`

The frontend (`apps/frontend/`) is a delivery layer over the backend's API, not a domain context of
its own, so it has no `CONTEXT.md`.
